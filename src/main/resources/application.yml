spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: system
    password: systempass
    url: jdbc:h2:tcp://localhost/~/java-aug-18

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties.hibernate.jdbc.lob.non_contextual_creation: true